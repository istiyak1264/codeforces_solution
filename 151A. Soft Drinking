#include <stdio.h>
int main() 
{
    int n, k, l, c, d, p, nl, np;
    scanf("%d %d %d %d %d %d %d %d", &n, &k, &l, &c, &d, &p, &nl, &np);
    printf("%d\n", (int)(fmin(fmin((float)(k * l) / nl, (float)(c * d)), (float)p / np) / n));
    return 0;
}
