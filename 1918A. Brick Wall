#include<stdio.h>
int main()
{

    long t,m,n;
    //taking input from the user.
    scanf("%ld", &t);

    //calculating the stability of wall.
    while(t--)
    {
        scanf("%ld %ld", &n,&m);
        printf("%ld\n", n*(m / 2));
    }
    return 0;
}
